{% extends 'base.html.twig' %}
{% block title %}Accueil -
	{{ parent() }}
{% endblock %}

{% block body%}
	<div class="container mt-5">
		<div class="row">
			<div class="col-md-12 text-center">
				<h1 class="display-4"> {{ 'home.welcome' | trans}} </h1>
				<p class="lead">{{ 'home.description' | trans}}</p>
			</div>
		</div>
	</div>
{% endblock %}
