{% extends 'base.html.twig' %}

{% block title %}
	Expensive burgers -
	{{ parent() }}
{% endblock %}

{% block body %}
	<div class="w-75 mx-auto m-5">

		<div class="row row-cols-2">
			<h1>
				Bienvenue sur la liste des burgers les plus chère !
			</h1>
			<form action="{{ path('burger_expensive') }}" method="get" class="d-flex mb-4">
				<input type="text" name="limit" class="form-control me-2" placeholder="Entrez chiffre" value="{{ limit }}">
				<button type="submit" class="btn btn-primary">Rechercher</button>
			</form>

		</div>


		{% include 'components/burgersDisplay.html.twig' with { 'burgers' : burgers} %}

	</div>
{% endblock %}
